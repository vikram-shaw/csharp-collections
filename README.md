# C# Generic Collections – UML Diagram

This repository contains a UML diagram that visualizes the hierarchy and relationships within **C# Generic Collections** in the .NET framework.

## 📌 UML Diagram (SVG)

Below is the SVG version of the diagram for crystal-clear zooming:

![C# Generic Collections UML Diagram](./Collections.drawio.svg)

> Make sure the SVG file name matches the reference above.

---

## 📄 What This Diagram Covers

This diagram focuses **exclusively on Generic Collections**, including:

### **🔷 Core Interfaces**
- `IEnumerable<T>`
- `ICollection<T>`
- `IList<T>`
- `IReadOnlyCollection<T>`
- `IReadOnlyList<T>`
- `IDictionary<TKey, TValue>`
- `IReadOnlyDictionary<TKey, TValue>`
- `ISet<T>`